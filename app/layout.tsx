import type { Metadata, Viewport } from "next";
import type { ReactNode } from "react";
import "./globals.css";

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
};

export const metadata: Metadata = {
  title: "C.SeifEddine | Full Stack Developer",
  description:
    "Premium portfolio for C.SeifEddine, Full Stack Developer and Information Systems Engineer from Algeria.",
  metadataBase: new URL("https://github.com/phantome001")
};

export default function RootLayout({
  children
}: Readonly<{
  children: ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body suppressHydrationWarning>{children}</body>
    </html>
  );
}
